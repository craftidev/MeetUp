<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/normalize.css') }}">
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>

    <nav>
        {% block nav %}
            {% if app.user %}
                <a href="{{ path('list_main') }}">Accueil</a>
                <a href="{{ path('list_main') }}">Campus TODO</a>
                <a href="{{ path('list_main') }}">Campus TODO</a>
                <a href="{{ path('user_modifprofil') }}">Mon profil</a>
                <a href="{{ path('app_logout') }}">Se deconnecter({{ app.user.pseudo }})</a>
                
                {% if app.user.roles[0] == 'ROLE_ADMIN' %}
                    <a href="{{ path('list_main') }}">Admin TODO</a>
                {% endif %}
                {% else %}
                    La connexion est obligatoire.<br />
                    TODO delete:<br />
                    ADMIN -> login: a@a.a mdp: a<br />
                    USER -> login: un@un.un mdp: un (ou deux/trois/quatre a la place de 'un') <br />
            {% endif %}
        {% endblock %}
    </nav>

    <body>
        {% block body %}{% endblock %}
    </body>
</html>
